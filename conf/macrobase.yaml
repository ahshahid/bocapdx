
server:
    rootPath: /api/*

#macrobase.loader.db.user: jags
#macrobase.loader.db.password: jags

logging:
  level: INFO

  # Logger-specific levels.
  loggers:
    "macrobase": TRACE 
    "org.glassfish.jersey.logging.LoggingFeature": DEBUG
server:
    applicationConnectors:
        - type: http
          port: 9090
    adminConnectors:
        - type: http
          port: 9091

macrobase.analysis.minSupport: 0.01
macrobase.analysis.minOIRatio: 2
macrobase.loader.db.url: "34.70.196.110:1527"
